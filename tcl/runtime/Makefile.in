#
# Makefile --
#
# Makefile for Extended Tcl package library.  Also generates help files
# for both standard and Extended Tcl.  This assumes that Tcl has compiled
# successfully, as it is run here.
#------------------------------------------------------------------------------
# Copyright 1992-1993 Karl Lehenbauer and Mark Diekhans.
#
# Permission to use, copy, modify, and distribute this software and its
# documentation for any purpose and without fee is hereby granted, provided
# that the above copyright notice appear in all copies.  Karl Lehenbauer and
# Mark Diekhans make no representations about the suitability of this
# software for any purpose.  It is provided "as is" without express or
# implied warranty.
#------------------------------------------------------------------------------
# $Id: Makefile,v 2.6 1993/06/24 07:32:30 markd Exp markd $
#------------------------------------------------------------------------------
#

include ../Config.mk
include ../config/${TCL_CONFIG_FILE}
SHELL=/bin/sh

#------------------------------------------------------------------------------

TCL.TLIB       = ../tclmaster/tcl.tlib
TCL.TNDX       = ../tclmaster/tcl.tndx
TCLINIT.TCL    = ../tclmaster/TclInit.tcl
BUILDHELP.TCL  = ../tclmaster/buildhelp.tcl
BUILDIDX.TCL   = ../tclmaster/buildidx.tcl
LOADOUSTER.TCL = ../tclmaster/loadouster.tcl
MAN            = ../tclmaster/man
HELP           = ../tclmaster/help

#------------------------------------------------------------------------------

TCL_SRCS=arrayprocs.tcl   compat.tcl       convlib.tcl      edprocs.tcl     \
         forfile.tcl      forrecur.tcl     globrecur.tcl    help.tcl        \
         profrep.tcl      pushd.tcl        setfuncs.tcl     showproc.tcl    \
         stringfile.tcl   tkdemo.tcl       tcllib.tcl       fmath.tcl       \
         tclshell.tcl \
         parray.tcl       ../${TCL_UCB_DIR}/library/parray.tcl

#------------------------------------------------------------------------------

all: ${TCLINIT.TCL} ${BUILDHELP.TCL} ${BUILDIDX.TCL} ${LOADOUSTER.TCL} \
     ${TCL.TLIB} ${MAN}

${TCLINIT.TCL}: TclInit.tcl
	-rm -f ${TCLINIT.TCL} 
	cp TclInit.tcl ${TCLINIT.TCL}

${BUILDHELP.TCL}: buildhelp.tcl
	-rm -f ${BUILDHELP.TCL}
	cp buildhelp.tcl ${BUILDHELP.TCL}

${BUILDIDX.TCL}: buildidx.tcl
	-rm -f ${BUILDIDX.TCL}
	cp buildidx.tcl ${BUILDIDX.TCL}

${LOADOUSTER.TCL}: loadouster.tcl
	-rm -f ${LOADOUSTER.TCL}
	cp loadouster.tcl ${LOADOUSTER.TCL}

${TCL.TLIB}: ${TCL_SRCS} ${BUILDIDX.TCL} ${TCLINIT.TCL}
	-rm -f ${TCL.TLIB} ${TCL.TNDX}
	cat ${TCL_SRCS} | egrep -v '^#$$|^#  *|^#-|^#=|^  *#' > $@
	../runtcl -c "buildpackageindex ${TCL.TLIB}"

${MAN}: ${TCLINIT.TCL} ${TCL.TLIB}
	if [ ! -d ${MAN} ] ; then \
	    ../runtcl ../tools/cpmanpages.tcl TCL ;\
	fi

#------------------------------------------------------------------------------
# Build help for Tcl & TclX.

buildtclhelp: ${TCLINIT.TCL} ${TCL.TLIB}
	rm -rf ${HELP}
	mkdir ${HELP}
	../runtcl ../tools/bldmanhelp.tcl ../${TCL_UCB_DIR}/doc \
                  ../tools/tclmanpages ${HELP} Tcl.brf
	../runtcl buildhelp.tcl -b TclX.brf ${HELP} ../man/TclX.man

#------------------------------------------------------------------------------

clean:
	-rm -f ${TCL.TLIB} ${TCL.TNDX} ${TCLINIT.TCL}
	-rm -f ${BUILDHELP.TCL} ${BUILDIDX.TCL} ${LOADOUSTER.TCL}
	-rm -rf ${MAN}
